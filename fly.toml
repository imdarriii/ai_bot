app = "ai-google-meet-v2"
primary_region = "fra"  # Frankfurt (close to Recall eu-central-1)

[http_service]
  internal_port = 8080
  force_https = true

[[http_service.checks]]
  path = "/health"
  interval = "30s"
  timeout = "5s"
